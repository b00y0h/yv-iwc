import { describe, expect, it } from 'vitest'

describe('index', () => {
  it('should pass CI', () => {
    // This is a dummy test to ensure the CI passes
    expect(true).toBe(true)
  })
})

export {}
